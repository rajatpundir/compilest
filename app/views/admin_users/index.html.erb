<% @page_title = "Manage Users" %>
<div class="center page-header">
  <h2>Manage Users</h2>
  <%= pluralize(@admin_users.size, 'user') %> found
  <div class="center">
    <%= link_to("Register User", new_admin_user_path, :class => 'btn btn-primary') %>
    <%= link_to("Reset Everything", admin_users_reset_path, :class => 'btn btn-danger') %>
  </div>
</div>
<div>
  <table class="table table-bordered table-striped table-hover table-condensed">
    <tr>
      <th>Year</th>
      <th>Branch</th>
      <th>Section</th>
      <th>Roll number</th>
      <th>Name</th>
      <th>Score</th>
      <th>Username</th>
      <th>Actions</th>
    </tr>
    <% @admin_users.each do |admin_user| %>
        <tr>
          <td><%= admin_user.year %></td>
          <td><%= admin_user.email %></td>
          <td><%= admin_user.section %></td>
          <td><%= admin_user.roll_number %></td>
          <td><%= admin_user.name %></td>
          <td><%= admin_user.score %></td>
          <td><%= admin_user.username %></td>
          <td>
            <%= link_to('Details', admin_user_path(admin_user), :class => 'btn btn-primary btn-sm') %>
            <%= link_to('Edit', edit_admin_user_path(admin_user), :class => 'btn btn-info btn-sm') %>
            <%= link_to('Delete', delete_admin_user_path(admin_user), :class => 'btn btn-danger btn-sm') %>
          </td>
        </tr>
    <% end %>
  </table>
</div>
